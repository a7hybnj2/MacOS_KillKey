#!/usr/bin/env python
#import subprocess #I read that this is *safer*
import os
from config import *
#subprocess.run(["ls", "-la"]) #used if subprocess
print(device_uuid)
#os.system('system_profiler SPUSBDataType | grep')